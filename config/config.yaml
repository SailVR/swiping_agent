# 大模型配置
llm:
  provider: "dashscope"
  model: "qwen-turbo"
  api_key: "${DASHSCOPE_API_KEY}"
  temperature: 0.1
  max_tokens: 2048

# 数据库配置
database:
  path: "./data/credit_bonus.db"

# NL2SQL配置
nl2sql:
  num_examples: 3   # Few-shot示例数量

# 记忆配置
memory:
  long_term_db: "./data/long_term_memory.db"  # 长期记忆数据库路径
  short_term_max_tokens: 1000  # 短期记忆最大token数
  compression_threshold: 10  # 超过10条消息开始压缩
  auto_extract_knowledge: true  # 会话中自动提取知识（6轮对话后）

# 联网搜索配置（DeepSearch）
search:
  tavily_api_key: "${TAVILY_API_KEY}"  # Tavily API Key，申请地址：https://tavily.com
  max_results: 5  # 每次搜索返回最大结果数
